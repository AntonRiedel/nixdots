session_name: analysis
windows:
  - window_name: ALICE
    panes:
      - shell_command: distrobox enter alice

  - window_name: ALIEN
    panes:
      - shell_command: distrobox enter alice -- bash -c 'cd $HOME/alice && alienv enter JAliEn-ROOT/latest; exec bash'
  - window_name: LOCAL
