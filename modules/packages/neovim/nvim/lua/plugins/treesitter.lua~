return {
	"nvim-treesitter/nvim-treesitter",
	lazy = false,
	build = false, -- never run :TSUpdate, nix manages parsers
	config = function()
		require("nvim-treesitter.configs").setup({
			auto_install = false, -- nix manages all grammars
			highlight = { enable = true },
			indent = { enable = true },
			incremental_selection = {
				enable = true,
				keymaps = {
					init_selection = "<C-space>",
					node_incremental = "<C-space>",
					scope_incremental = false,
					node_decremental = "<bs>",
				},
			},
		})
	end,
}
